package topicDlearningaids.nov2018hlp2;

import java.util.LinkedList;

public class FIleManagement {
    public static void main(String[] args) throws Exception {
        // outline an advantage of using a library
        // we do not have to replicate standard functionality
        // even if we wanted to, using the library is probably better anyways because it has been tested and around for way longer.
        LinkedList<Arrival> runway1 = new LinkedList<Arrival>();

    }
}
